# Creative-Ai project status

Last reviewed: 2026-07-21

Keep this file free of credentials, internal addresses, and other machine-specific values. Update it after material deployment or architecture changes.

## Verified rollout state

- The Laravel/Filament application is the active codebase; the retired static-site files have been removed from the current branch and remain recoverable from Git history.
- Local development runs through the root Docker Compose stack with PostgreSQL, Redis, migrations, the website, and the queue worker.
- Pull requests run tests and an isolated stack smoke test.
- A merge-triggered push to `main` publishes and smoke-tests one immutable GHCR image.
- Staging and production use separate Compose Manager stacks and the same approved image digest.
- The public reverse proxy, canonical-host redirect, HTTPS assets, indexing policy, readiness endpoint, and administrator login were verified during cutover.
- Persistent storage and database behavior were verified, and scheduled backups are managed outside this repository.
- PHP, Livewire, and the track form share a tested 100 MiB music-upload limit, with a 105 MiB PHP request allowance for multipart overhead.
- Music imports extract embedded metadata and covers with filename fallback, albums are first-class ordered releases, cover suggestions use explainable shared-tag ranking, and manual playlists have drag-sortable sequencing.
- Music includes background technical analysis, waveform and duplicate/health data, rich smart-playlist rules and snapshots, explicit embedded-cover handling, public album/track discovery with cross-media recommendations, and a persistent browser queue.
- Playlists now mirror automatic artwork collections with managed tag/theme generation and AI-assisted creation. Normalized album matching groups multi-artist tracks by release metadata during upload, with an administrator repair action for older imports.
- The former public backend is retained temporarily as an unrouted rollback target.

The exact deployed image references and infrastructure values live in Compose Manager and ignored local environment files. Inspect them only when required and never print their secret-bearing contents.

## Current development checkpoint

- Pull request #11 merged the release-centric album/track work into `main`. Its merge-triggered workflow published an immutable GHCR candidate and successfully smoke-tested that exact digest. It has not been deployed to staging or production.
- Album publication now grants inherited playability to current and future member tracks without listing them as standalone releases. Album pages own their ordered track listings; direct track/audio access, public playlists, recommendations, and smart-playlist eligibility use effective album-or-track availability. The persistent player exposes separately grouped album, playlist, and standalone-track choices.
- The administrator track library defaults to collapsible album groups. Grouped mode deliberately loads every matching track so an album cannot be hidden by a saved per-page limit; ordinary pagination returns when grouping is disabled. Audio-health results poll every five seconds, retries clear stale state, and a draft album's configured embedded/gallery cover is evaluated independently from its intentionally private public URL.
- The expand-compatible migration adds explicit standalone publication fields, normalizes existing album members to album-only, changes the legacy default, and maintains the legacy publication fields as a compatibility mirror for a temporary old-image rollback. Do not perform music publication edits while an older image is restored.
- Pull request #12 merged the durable public artwork archive into `main`; its merge-triggered workflow also published and smoke-tested an immutable candidate without deploying it. Artwork now has permanent pages with process notes, prompt/context presentation, deterministic adjacent navigation, reciprocal music recommendations, artwork SEO and structured data, artwork sitemap coverage, and stable cursor-based gallery loading with an accessible link fallback.
- Pull request #13 merged public playlist and music discovery into `main`; its merge-triggered workflow published and smoke-tested the latest immutable candidate without deploying it. Playlists now have durable public pages and discovery cards; music pages have page-specific Open Graph metadata, public-only `MusicAlbum`, `MusicRecording`, and `MusicPlaylist` structured data, reciprocal artwork links, and complete album/playlist/track sitemap coverage.
- The current Journal lifecycle slice replaces direct publication fields with explicit Draft, Ready, Scheduled, and Published transitions; private brief/notes; computed readiness; secure saved-state previews; effective query-driven scheduling; public-content timestamps; richer `BlogPosting` metadata; corrected RSS; and accessible cover/date presentation. A due Scheduled post becomes public without a scheduler or a state-changing request, while every public surface uses the same fail-closed predicate.
- The Journal lifecycle migration is expand-compatible and keeps the legacy publication fields synchronized for image-only rollback. Keep Journal publication administration read-only while an older image is running.
- The connected-stories slice adds shared Journal tags, ordered typed links to artwork, collections, albums, playlists, and tracks, an administrator-only Connections workspace, reciprocal public Journal discovery, shared tag archive pages, and corresponding schema/sitemap coverage. Connection writes are transactional and public rendering requires both the story and source to satisfy their canonical effective-publication rules; malformed links fail closed. Its migration adds relation tables only and is safe to leave applied during image rollback, but an older image cannot maintain those connections.
- The Journal planning-tools slice adds administrator-managed reusable templates, safe source placeholders, public-source **Create Journal draft** actions, and a bounded **Story opportunities** queue with dashboard visibility. Generated posts are transactionally forced to Draft, connect their source at position one, optionally copy only public archive tags, never mutate source content or publication, and use bounded collision-safe slugs. The additive migration also supplies inverse `post_media` indexes for the opportunity queries.
- The current working-tree Journal planning improvement adds clearer **Blank draft**, **Create from content**, and **Manage templates** entry points; deliberate planning from saved private or future sources; verified private source-artwork snapshots for draft creation and connected-media cover replacement; and guarded post readiness when a connection is not yet public. A source snapshot is independently owned by the post and remains stable when its source later changes or becomes private.
- The same working-tree improvement adds administrator-owned Journal planning defaults with Off, Ask, and Automatic modes. All workflows default to Off. Enabled single-source actions create at most one connected private Draft, artwork bulk upload creates one ordered batch story, and music import creates at most one story per detected album rather than per member track. Automatically maintained collections/playlists are excluded, source work is retained if secondary Journal planning fails, and no assisted path invokes AI or changes Journal workflow or publication state.
- Audio delivery remains publication-aware progressive HTTP byte-range streaming through the application rather than adaptive HLS. New working-tree coverage exercises full and partial delivery, range and cache headers, invalid ranges, inherited album availability, and fail-closed unpublished audio without changing the native player's metadata preload behavior.
- The Journal history-safety slice adds baseline-backed immutable public-content revisions with private-field exclusion, audit-only connection snapshots, workflow/slug/trash audit entries, checksum-verified allowlist-only restore, stale-editor and stale-restore protection, direct canonical slug redirects and permanent tombstones, service-backed Trash/restore/permanent deletion, and browser revalidation for public Journal pages and covers. Trash and restore always leave the post private in Draft; permanent deletion retains private cover files and reserves every former public slug.
- Pull request #17 merged the Journal history-safety slice into `main`. Its merge-triggered workflow published and smoke-tested `ghcr.io/nicolasdwolfwood/creative-ai@sha256:8e01144420692aee46fb7807cbca8cd9d4635de09d3d8c2821776ed43709b957`; this candidate has not been deployed to staging or production.
- Pull request #18 merged the dormant Journal AI safety foundation into `main`. Its merge-triggered workflow published and smoke-tested `ghcr.io/nicolasdwolfwood/creative-ai@sha256:3c252405bee1f2d24f5d1ce50e20dd69f8ee1e755cff99133e1861d653653604`; this candidate has not been deployed to staging or production.
- The Journal AI foundation adds an immutable `post_ai_runs` ledger; exact acknowledged context manifests; source, request, provider, prompt, and schema fingerprints; safe provider request/token provenance; independent Journal text-model selection; pinned endpoint and credential identity; redirect rejection and HTTPS-only external processing; bounded one-attempt queue execution; cancellation, priority, explicit retry, and stale recovery; five versioned structured editorial contracts; and strict result normalization. Private editorial fields and public connected-media process context require separate opt-ins, effectively private media is excluded, and trusted instructions stay separate from untrusted writing.
- The current Journal AI assistant slice activates that foundation through a saved-post workspace for directions, outlines, editorial review, exact-passage improvement, and metadata suggestions. Every request requires an exact outbound-context and destination preview plus fresh acknowledgement. Results are escaped and inert by default; persistent outline, passage, and individually selected non-empty metadata application is limited to Draft/Ready posts, uses only normalized server-retained suggestions, rechecks contract/source/revision fingerprints under locks, creates one immutable `ai_apply` revision/application manifest, and supports revision-path-guarded undo. Slug, workflow, schedule, publication, featured placement, private notes, tags, cover source, and media connections remain protected.
- Studio operations now include an oldest-first Journal AI jobs queue with safe provenance, lineage, duration/token telemetry, cancel/prioritize controls, and acknowledgement-preserving review/retry links. The Studio dashboard exposes bounded aggregate work queues for failed variants, failed audio analysis, public alt/cover gaps, metadata awaiting review, effective Journal drafts, future schedules, and stale/failed Journal AI work without displaying stored error or context payloads.
- The Journal AI table and assistant application-manifest column are additive and older images ignore them. The assistant migration refuses a destructive rollback after an application has been audited. Before rolling back to any image that lacks `GenerateJournalAiRun`, cancel queued/processing work with the current image and keep its worker running until the superseded payloads are consumed as no-ops; otherwise the older worker cannot deserialize the job class. Journal AI administration must remain paused during that rollback.
- Local validation for the Journal lifecycle slice passed: 156 application tests with 1,202 assertions; Pint across 165 PHP files; frontend production build; both tracked Unraid configurations; and a healthy rebuilt PostgreSQL/Redis development stack with the migration applied.
- Local validation for connected stories passed: 166 application tests with 1,350 assertions; Pint across 176 PHP files; frontend production build; both tracked Unraid configurations; and a healthy rebuilt PostgreSQL/Redis development stack with the additive migration applied and public route smoke checks returning `200`.
- Local validation for Journal planning tools passed: 180 application tests with 1,536 assertions; Pint across 190 PHP files; frontend production build; both tracked Unraid configurations; a focused PostgreSQL 17 planning/opportunity run with 9 tests and 99 assertions; and a healthy rebuilt PostgreSQL/Redis development stack with the planning migration, all five opportunity indexes, and administrator routes present.
- Local validation for the dormant Journal AI foundation passed: 279 application tests with 2,390 assertions; Pint across 222 PHP files; frontend production build; both tracked Unraid configurations; a focused PostgreSQL 17 run of all 74 foundation, provider, privacy, normalization, and lifecycle tests with 496 assertions; and a healthy rebuilt PostgreSQL/Redis development stack with the additive migration applied. A final adversarial review found no blocker, high, or medium regression; all low-severity follow-ups were resolved with bounded streaming provider envelopes, sanitized stream-failure categories, and hard-timeout duration telemetry.
- Local validation for the Journal AI assistant and Studio operations slice passed: 310 application tests with 2,763 assertions; Pint across 233 PHP files; frontend production build; both tracked Unraid configurations; a focused PostgreSQL 17 run of 125 assistant, application, queue, dashboard, foundation, history, and lifecycle tests with 1,091 assertions; a clean disposable SQLite rollback and reapply of the assistant migration; and a healthy rebuilt PostgreSQL/Redis development stack with `/ready` healthy. A final adversarial review found no remaining blocker, high, medium, or low finding after the creation, application, undo, metadata, passage-regeneration, public-state, and cross-post invariants were tightened and covered by regression tests.
- Local validation for Journal history safety passed: 205 application tests with 1,894 assertions; Pint across 200 PHP files; frontend production build; both tracked Unraid configurations; a focused 46-test history/lifecycle/connection run with 619 assertions; a focused PostgreSQL 17 history/migration/admin/public/lifecycle run with 34 tests and 451 assertions; and a healthy rebuilt PostgreSQL 17/Redis development stack with the baseline migration applied and `/ready` healthy.
- A real multi-track draft album with an embedded cover was reanalyzed after the health fix. All 14 jobs completed, the table advanced without a manual refresh, and all 14 tracks finished healthy rather than retaining the false missing-cover warning.
- Local validation for the current audio and Journal planning working-tree improvement passed: 359 application tests with 3,165 assertions inside the Linux test image; Pint across all 252 PHP files; the frontend production build; and both tracked Unraid configurations. A disposable PostgreSQL 17 run passed 62 focused audio, planning, cover, privacy, lifecycle, and administrator workflow tests with 656 assertions. A forced two-worker PostgreSQL concurrency probe blocked both workers on the same source row, then produced exactly one draft while both calls resolved to the same post. The rebuilt PostgreSQL/Redis development stack is healthy, its one-shot migration exited successfully, `/ready` and `/` return `200`, Redis and storage writes are healthy, the queue worker is running, and the recent logs are clean. A live local audio request returned `206` with byte ranges, an exact partial body, inline delivery, and private revalidation caching. Focused human staging acceptance remains pending, including confirmation that the staging HTTPS proxy and native browser seeking preserve ranged audio.
- The remaining release gate is separately authorized staging deployment and focused acceptance of the newest immutable candidate produced from `main`. Do not deploy or promote any candidate without separate authorization, and do not promote to production until that exact digest is accepted on staging.

## Known follow-up work

1. **Release and staging acceptance:** required TOTP MFA, private publication-aware media delivery, security headers, accessible navigation/lightbox/player behavior, bundled admin visuals, private-media migration, and artwork variants passed the earlier development and staging/test acceptance. The album-first, artwork, playlist/music-discovery, Journal lifecycle, progressive audio-range contract, and assisted Journal planning changes still need separately authorized staging deployment and focused acceptance as one exact immutable digest before approval. That acceptance must include proxy-preserved `206` audio ranges; the create-from-content flow; stable private source-cover snapshots; private/future-source fail-closed behavior; default-off planning settings; one-story artwork-batch and per-album import grouping; and confirmation that publication and every AI request/application remain separate human-controlled actions.
2. **Album-track normalization and stored health:** the former cascade did not record whether an album member was intentionally released as a single. The new forward migration therefore resets every existing album member to album-only. After the next staging migration, review the standalone track list and explicitly re-enable only exceptional album tracks that should also appear as singles. Rerun **Analyze audio health** for album tracks carrying an older false missing-cover warning and verify inherited embedded/gallery covers and explicit no-cover choices settle correctly.
3. **Reboot readiness:** web and worker services use `restart: unless-stopped`, but Compose Manager stack autostart and external PostgreSQL/Redis startup ordering have not been proven with a controlled reboot. The migration job also has no bounded dependency retry.
4. **Credential maintenance:** rotate any credentials previously exposed during setup. Separate Redis ACL users remain preferred; the current shared-password rotation and durable ACL setup were deliberately deferred.
5. **Runtime parity:** local development currently uses Redis 7 while the deployed shared service uses Redis 8. Align and pin the supported runtime version in a dedicated maintenance change.

Items 1 and 2 are required before the first production promotion. Items 3 through 5 are recorded operational follow-ups rather than launch blockers for the current personal site.
